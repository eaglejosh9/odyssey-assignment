# Wrangler reads this file during `wrangler dev`. Copy to .dev.vars and
# paste your Neon connection string (same value as .env DATABASE_URL).
DATABASE_URL=postgresql://user:password@ep-your-host.neon.tech/neondb?sslmode=require
