# Intentionally empty — the Orval-generated subdirectories under src/generated/
# are committed. They are the runtime source of truth; the top-level files
# (menu.ts, orders.ts, customers.ts, settings.ts, stats.ts, schemas.ts) are
# thin alias re-exports that give the dashboard friendlier hook names.